package com.crm.repository;

import com.crm.entity.BonoConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BonoConfigRepository extends JpaRepository<BonoConfig, Integer> {
}